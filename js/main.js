document.addEventListener('DOMContentLoaded', () => {
    // Inicialização do Animate On Scroll (AOS)
    AOS.init({
        duration: 800,
        once: true,
        offset: 100,
        easing: 'ease-out-cubic'
    });
});
